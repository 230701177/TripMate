<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tripmate.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TripMate">
        
        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TripMate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity 
            android:name=".ui.auth.LoginActivity" 
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity 
            android:name=".ui.auth.SignupActivity" 
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main Flow -->
        <activity android:name=".ui.home.HomeActivity" android:exported="false" />
        <activity android:name=".ui.profile.ProfileActivity" android:exported="false" />
        <activity android:name=".ui.notification.NotificationActivity" android:exported="false" />

        <!-- Trip Flow -->
        <activity android:name=".ui.trip.TripDetailActivity" android:exported="false" />
        <activity android:name=".ui.expense.ExpenseActivity" android:exported="false" />
        <activity android:name=".ui.checklist.ChecklistActivity" android:exported="false" />
        <activity android:name=".ui.event.EventActivity" android:exported="false" />
        <activity android:name=".ui.collaboration.CollaborationActivity" android:exported="false" />

    </application>

</manifest>
